# CAMP-HNSW: runtime dependencies for the reproduction pipeline in scripts/.
#
# NOTE: hnswlib and ours_backend are intentionally NOT listed here. They are
# this repository's own modified engine (src/hnswlib, src/ours_backend.cpp),
# not PyPI packages -- `pip install hnswlib` would silently pull the
# unmodified upstream library and break every result. Build them from source
# instead (this is what `scripts/run_all.sh` does automatically):
#   pip install --no-build-isolation -e src/hnswlib
#   pip install --no-build-isolation -e src

# Core numerical / data stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
h5py>=3.8

# Build-time dependency for compiling src/hnswlib and src/ours_backend.cpp
pybind11>=2.10

# Optional: only used by scripts/common.py as a fallback to compute exact
# ground truth when a dataset's .hdf5 file has no precomputed calibration
# query neighbors. Every dataset used in the paper (Table 1) already ships
# precomputed ground truth, so this path is rarely exercised; a pure-numpy
# fallback runs automatically if faiss isn't installed.
# faiss-cpu>=1.7
